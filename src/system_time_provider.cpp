#include "system_time_provider.h"

#include <ctime>

uint8_t SystemTimeProvider::hour() { return 0; }

uint8_t SystemTimeProvider::minute() { return 0; }

uint8_t SystemTimeProvider::second() { return 0; }
